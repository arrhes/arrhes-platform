{
  "name": "arrhes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter='api' --filter='platform' run dev",
    "build": "pnpm --filter='metadata' --filter='api' --filter='platform' run build"
  },
  "workspaces": [
    "./packages/*"
  ]
}